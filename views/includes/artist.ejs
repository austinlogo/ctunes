<% if (account.username != user.user) { %>
	<div class="artist-item">	
		<a href="/<%=user.user %>">
			<div class="artist-info">
				<p class="artist-name"><%= user.user %></p>									
			</div>
		</a>
		<div class="icon-container" id="icon-container-<%= user.user %>">
			<div class="icon" id="following-<%= user.user %>">
				+
			</div>
		</div>
	</div>
<% } %>